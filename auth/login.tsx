

















export default function Login(props:any) {
  return (
    <dialog>
        <form>
            <dialogirigger>
                <button variant="default" className="text-gray-500 hover:text-gray-700">
                    Login
                </button>
            </dialogirigger>
             <button variant="default" className="absolute top-2 right-2 text-gray-500 hover:text-gray-700" onClick={() => props.setOpen(false)}>
        
    </dialog>

